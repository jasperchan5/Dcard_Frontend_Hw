{
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install --only=dev && npm install && yarn build",
    "frontend": "cd frontend && yarn start"
  },
  "dependencies": {
    "body-parser": "^1.19.2",
    "cors": "^2.8.5",
    "curl": "^0.1.4",
    "dotenv": "^16.0.0",
    "dotenv-defaults": "^5.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.15",
    "octonode": "^0.10.2"
  }
}
